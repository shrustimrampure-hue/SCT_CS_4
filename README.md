# ⌨️ Keyboard Event Logger (Educational)

A simple Python program using the `pynput` library to demonstrate how to capture keyboard input events.  
This project is intended **for educational purposes only** to learn about keyboard event handling in Python.

---

## 🔑 Features
- **Key Press Detection**: Captures both character keys and special keys (Shift, Ctrl, etc.).
- **Event Logging**: Writes pressed keys to a file (`log.txt`) for demonstration.
- **Real-Time Feedback**: Prints pressed keys in the terminal for debugging.

---

## 🚀 How to Run

### Prerequisite
- Install [Python](https://www.python.org/downloads/) (3.x recommended).
- Install `pynput`:
  ```bash
  pip install pynput
